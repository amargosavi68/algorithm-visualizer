import React from 'react'
import OperationChoice from './OperationChoice';

const Array = (props) => {
     return (
          <div>
               <OperationChoice />
               <h3>Array is here</h3>
          </div>
     )
}

export default Array;
