import React from 'react'
import OperationChoice from './OperationChoice'

const Stack = (props) => {
     return (
          <div>
               <OperationChoice />
          </div>
     )
}

export default Stack;
